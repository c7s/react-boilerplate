import { hot } from 'react-hot-loader';
import { App as IntersectedApp } from './modules/common/App';

// Import assets here (such as external css, html, fonts, etc)

export const App = hot(module)(IntersectedApp);
