module.exports = {
    port: 3000,
    /**
     * Switch to 'true' to get production build.
     * Only for debugging differences between production and development builds.
     */
    isBuild: false,
};
