import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { HotApp } from './App';

ReactDOM.render(React.createElement(HotApp, { name: 'Name2' }), document.getElementById('root'));
